# Session: 2026-07-14

## Completed
- Diagnosed and resolved a production outage on `avschats.nodayoby.online` (prod host, `wwebjs` provider): all chats stopped sending/receiving for every user simultaneously.
- Root cause identified from `docker compose logs backend`: repeated `ProtocolError: Runtime.callFunctionOn timed out` — a hung Puppeteer/Chrome page inside the whatsapp-web.js session (container had 4 weeks of continuous uptime with no restart). Any request awaiting `wbot` (send message, mark-as-read) hung until nginx's proxy timed out at 504; that 504 response bypasses Express's `cors()` middleware, so the browser reported it as a CORS violation — a red herring that initially looked like a CORS config regression in `backend/src/app.ts`.
- Verified CORS config itself was correct throughout via manual `curl -X OPTIONS` preflight from the server (got clean `204` + correct `Access-Control-Allow-Origin` headers) — confirmed the earlier CORS-looking errors were symptoms, not a real `corsOptions`/`FRONTEND_URL` misconfiguration.
- Fixed via `docker compose restart backend` (no rebuild). `.wwebjs_auth/` is a host-mounted volume, so `LocalAuth` reconnected automatically — logs confirmed `Session: AVS AUTHENTICATED` → `Session: AVS READY` ~4s later, no QR re-scan needed. Backlogged WhatsApp messages delivered in a burst after reconnect (expected multi-device queueing behavior, not a bug).
- Saved two memory files for future sessions:
  - `~/.claude/projects/-Users-sviat-whaticket/memory/project_wwebjs_puppeteer_freeze_incident.md` — full incident writeup and diagnostic steps for recurrence.
  - `~/.claude/projects/-Users-sviat-whaticket/memory/feedback_cors_504_masquerade.md` — general rule: CORS error + 504 on the same request means check backend health/logs before touching CORS config.
- Also noted (no fix applied): browser console 403s on `pps.whatsapp.net` avatar images are benign — those are WhatsApp's own signed CDN URLs (`Avatar src={ticket?.contact?.profilePicUrl}` in `frontend/src/components/TicketListItem/index.js:103`) expiring after being cached a long time; unrelated to the outage.

## In Progress
- Nothing left mid-implementation. Investigation and hotfix (container restart) are complete; the app was confirmed working again by the user after the restart.

## Next Steps
1. Decide whether to implement a permanent fix for the underlying Puppeteer-freeze risk (no auto-healing currently exists in `backend/src/providers/WhatsApp/Implementations/wwebjs.ts:501-518`). Options discussed, none implemented yet:
   - Scheduled nightly `docker compose restart backend` via cron (simplest, blunt).
   - In-code watchdog: periodically call `wbot.getState()`; if it times out repeatedly, recreate the Puppeteer client programmatically.
   - Set an explicit `protocolTimeout` on the Puppeteer launch config and a memory limit / `shm_size` on the `backend` service in `docker-compose.yaml`, so Chrome fails fast and visibly instead of hanging silently.
2. User explicitly said not to change anything this session — revisit when they're ready to prioritize it.

## Decisions Made
- Confirmed `docker compose restart backend` (not `--build`) is the correct/safe response to this failure mode: same image, `.wwebjs_auth/` volume persists, no QR re-auth required.
- Chose not to implement any permanent auto-recovery mechanism yet per explicit user instruction ("пока ничего не меняй") — diagnosis and documentation only.

## Blockers
- None currently. Production is back to normal as of end of session.
